# Blender Sources

Store editable `.blend` source files here.

Recommended ownership:

```text
characters/
  chug.blend
  enemy_base.blend
environments/
  arena.blend
```

Export game-ready files to `assets/models/`. The runtime currently recognizes `assets/models/characters/chug.glb` and `enemy.glb`.
